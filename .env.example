VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Organizer login is now handled by Supabase Auth (email + password).
# Create the organizer account in the Supabase dashboard:
#   Authentication → Users → Add user (set "Auto Confirm User").
# There is no longer a VITE_ORGANIZER_PASSWORD.
